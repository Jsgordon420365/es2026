# Copy this file to search_everything.env (or .env) and edit the values
# below. search_everything.env is gitignored so your local path never
# ends up in version control. Alternatively, run the setup wizard:
#   python -m search_everything.wizard
EVERYTHING_ES_PATH=es.exe
EVERYTHING_DEFAULT_LIMIT=100
EVERYTHING_LOG_LEVEL=INFO
